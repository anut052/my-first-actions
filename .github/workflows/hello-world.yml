name: Hello World Workflow


on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello, World! ยินดีต้อนรับสู่ GitHub Actions"